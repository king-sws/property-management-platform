import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['api.dicebear.com', 'images.unsplash.com', 'q2hgul6sqoepyawk.public.blob.vercel-storage.com'],
  },
};

export default nextConfig;
